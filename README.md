# Python Utility

Python utility project with environment validation.

## Requirements

- Python 3.8+

## Setup & Installation

Install dependencies and pre-commit hooks:

```bash
pip install pre-commit
pre-commit install
pre-commit run --all-files
```
